Inline Lucide glyph that inherits currentColor and the variant's stroke width; use for every icon in the system.

```jsx
<Icon name="clock" size={18} />
<Icon name="arrow-right" mirror />   {/* flips in RTL */}
```

Set `mirror` only on directional glyphs. Clock faces, media controls, logos and the KNET mark never mirror.
